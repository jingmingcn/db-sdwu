
# 数据库系统概论  
## 关系代数基础  
讲授教师：XXX  
专业：计算机科学  

---

## 什么是关系代数？

### 定义

关系代数是对关系型数据库进行**查询和操作**的一种形式系统，使用一组**代数式运算符**对“关系”（表格）进行处理。

### 特点

- 操作对象：关系（表）
- 运算结果：仍是关系
- 类似数学集合操作
- 是 SQL 的理论基础

---

## 关系代数的运算类型

### 基本运算（核心操作）

| 运算符 | 含义         | 类似 SQL             |
|--------|--------------|----------------------|
| σ      | 选择         | `WHERE` 子句         |
| π      | 投影         | `SELECT` 字段        |
| ∪      | 并           | `UNION`              |
| −      | 差（差集）   | `EXCEPT`             |
| ×      | 笛卡尔积     | 多表连接的基础       |

### 衍生运算（组合简化）

| 运算符 | 含义       | 说明                   |
|--------|------------|------------------------|
| ⋈      | 连接       | 通常用于多表查询       |
| ⋈      | 自然连接   | 自动按同名属性连接     |
| ÷      | 除法       | 用于“全部满足”查询     |

---

## 示例关系

我们引入三个示例关系：

**Student(SID, SName, Major, Age)**  
**Enroll(SID, CID)**  
**Course(CID, CName)**  

样例数据如下：

### Student 表  
| SID | SName | Major | Age |
|-----|-------|--------|-----|
| 101 | 张三  | CS     | 20  |
| 102 | 李四  | Math   | 22  |
| 103 | 王五  | CS     | 21  |

---

## 运算1：选择（σ）

### 语法：

```
σ 条件 (关系)
```

### 示例：

查询所有主修“CS”的学生：

```
σ Major = 'CS' (Student)
```

结果：

| SID | SName | Major | Age |
|-----|-------|--------|-----|
| 101 | 张三  | CS     | 20  |
| 103 | 王五  | CS     | 21  |

💡 类似 SQL 的 `WHERE Major = 'CS'`。

---

## 运算2：投影（π）

### 语法：

```
π 属性列表 (关系)
```

### 示例：

查询所有学生的姓名和专业：

```
π SName, Major (Student)
```

结果：

| SName | Major |
|-------|--------|
| 张三  | CS     |
| 李四  | Math   |
| 王五  | CS     |

💡 消除重复值（Set 模型）。

---

## 运算3：并（∪）

### 语法：

```
关系1 ∪ 关系2
```

前提：两个关系**属性数相同，类型一致**

### 示例：

查询CS专业和Math专业的学生：

```
(σ Major='CS' (Student)) ∪ (σ Major='Math' (Student))
```

💡 类似 SQL 的 `SELECT ... UNION SELECT ...`

---

## 运算4：差（−）

### 语法：

```
关系1 − 关系2
```

### 示例：

查询CS专业但不是21岁的学生：

```
σ Major='CS' (Student) − σ Age=21 (Student)
```

---

## 运算5：笛卡尔积（×）

### 语法：

```
关系1 × 关系2
```

### 示例：

```
Student × Course
```

结果是每位学生配对每门课，结果记录数是两表记录数相乘。

⚠️ 实际应用中较少直接使用，常用于**连接**运算的底层构造。

---

## 运算6：连接（⋈）

### 定义：

连接两个关系中**满足某种条件**的元组。

### 语法：

```
关系1 ⋈_条件 关系2
```

### 示例：

```
Student ⋈_{Student.SID = Enroll.SID} Enroll
```

结果将学生信息与选课信息匹配起来。

---

## 运算7：自然连接（⋈）

### 特点：

- 自动匹配同名字段
- 自动去除重复列

### 示例：

```
Student ⋈ Enroll
```

⚠️ 假设两个表中都有 SID 字段

---

## 运算8：除法（÷）

### 作用：

处理“**所有**满足条件”的查询。即：在 A ÷ B 中，找出在 A 中**关联了所有 B 的值**的元素。

### 示例：

> 查询那些选修了“所有课程”的学生

```
π SID (Enroll) ÷ π CID (Course)
```

---

## 综合示例题

**问题**：查询选修了“数据库”课程的学生姓名

### 步骤：

1. 找出数据库课程的 CID  
   → `σ CName='数据库' (Course)`

2. 找出 Enroll 中选修了该课程的 SID  
3. 通过连接找出学生姓名

---

### 关系代数表达式：

```
π SName (
  (Student ⋈ Enroll) ⋈ 
  (σ CName = '数据库' (Course))
)
```

---

## 课堂练习

### 练习1：

关系：Employee(EID, EName, Dept, Salary)

> 写出关系代数表达式，找出“工资大于5000的员工姓名和部门”。

---

### 练习2：

关系：Works(EID, PID, Hours), Project(PID, PName)

> 写出关系代数表达式，查询“参与了编号为P1项目的所有员工编号”。

---

## ✅ 练习参考答案

### 练习1答案：

```
π EName, Dept (σ Salary > 5000 (Employee))
```

---

### 练习2答案：

```
π EID (σ PID = 'P1' (Works))
```

---

## 思维拓展：连接 vs 笛卡尔积

- 笛卡尔积（×）：会生成**所有组合**，信息过多，效率低  
- 连接（⋈）：在笛卡尔积基础上**加条件筛选**，实用性强  
- 自然连接：简化连接表达，推荐用于同名字段连接

---

## 小结

- 关系代数定义了一组对关系型数据库进行操作的代数系统
- 掌握选择、投影、连接、除法等操作对于理解 SQL 很关键
- 理解运算顺序、结合多种操作组合完成复杂查询
- 多做练习，熟悉表达式书写与转换思路

---

📚 推荐实践：
- 画出中间结果表，辅助理解运算过程
- 与 SQL 查询语句进行对照转换
